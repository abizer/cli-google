A simple Python function / script that returns a list of Google search results. 

Accepts a query string, a number of results to get and a dict of url options, which is composed 
of anything you wish to modify the URL with that Google accepts as url options. 

Todo: 
	1. Implement more options, such as returning names / descriptions
	2. Return results from arbitrary number of pages [Complete - 8/8/12]
	3. Implement lxml as a substitute to BeautifulSoup
